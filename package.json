{
  "name": "ton-assault-game",
  "version": "1.0.0",
  "description": "Space shooter game with Telegram integration",
  "main": "index.html",
  "scripts": {
    "start": "if [ -f server.js ]; then node server.js; else npx serve -s . -l 3000; fi",
    "dev": "npx live-server --port=3000",
    "test": "echo 'No tests specified'",
    "build": "echo 'Static site - no build step'"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["game", "telegram", "space-shooter", "firebase"],
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Blaxk44/TONASSUALTGAME.git"
  }
}